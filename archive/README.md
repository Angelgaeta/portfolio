# Archive

Ce dossier contient des snapshots et backups historiques du projet.

Contenu deplace depuis la racine:
- `_backup/`
- anciens fichiers `*.bak`
- fichiers `pre_restore` et `pre_revert`

Objectif: garder une racine propre tout en conservant l'historique Git.
